# Challenge 3 — Jump Host & SSH Config

Bastion access and `~/.ssh/config`.

## Objective

Document what you implemented, not only what the lab asked for.

## Environment

- OS: `<your OS>`
- Multipass instance(s): `<names>`
- IP(s): `<IPs>`

## Implementation

```bash
# Add the actual commands you used.
```

## Configuration

Place reusable configuration files under the corresponding `configs/` directory.

## Validation

Add the exact validation command and a short explanation of the result.

```bash
# Example
```

## Evidence

Add a screenshot to:

```text
screenshots/
```

Recommended naming:

```text
challenge-XX-<topic>.png
```

## Problems & Fixes

Document at least one troubleshooting point if you encountered one.

> The goal is to make this page reproducible from your actual implementation.
